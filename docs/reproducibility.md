# Reproducibility

The public release intentionally separates full reproduction from aggregate analysis-only reproduction. Users must obtain third-party data directly from PhysioNet and must not place raw files under version control. Frozen configuration and result locks are included; model hashes are included where non-sensitive, but weights are not redistributed.
